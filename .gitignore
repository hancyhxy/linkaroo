# macOS
.DS_Store

# Swift Package Manager
.build/
.swiftpm/
Packages/
Package.resolved
*.xcodeproj
!LinkarooApp/Linkaroo.xcodeproj
xcuserdata/

# SwiftPM build artifacts that occasionally leak into the working tree
ABDesignSystem/resource_bundle_accessor.*

# Xcode user-specific
*.xcuserstate
*.xcuserdatad/
DerivedData/

# Editor cruft
.vscode/
.idea/
*.swp
*~

# Node (if any web tooling sneaks in)
node_modules/

# Large binary files (GitHub single-file limit is 100MB)
*.mov
*.mp4
*.zip

# Local secrets (defensive — no secrets expected in this repo)
*.local
.env
.env.*

# Superseded v1 docs kept locally for personal reference; not part of
# the public product narrative. README does not reference them.
archive/

# Original avatar source photos — not committed to repo. The 5 actually
# used by the app are bundled under ABDesignSystem/Sources/Resources/Avatars/.
docs/user_pic/
