# macOS
.DS_Store

# Swift Package Manager
.build/
.swiftpm/
Packages/
Package.resolved
*.xcodeproj
xcuserdata/

# Xcode user-specific
*.xcuserstate
*.xcuserdatad/
DerivedData/

# Editor cruft
.vscode/
.idea/
*.swp
*~

# Node (if any web tooling sneaks in)
node_modules/

# Large binary files (GitHub single-file limit is 100MB)
*.mov
*.mp4
*.zip

# Local secrets (defensive — no secrets expected in this repo)
*.local
.env
.env.*

# Throwaway prototype sandbox — local only, never committed.
# Spec re-implementations of Pages/* live here while we test spec.md
# fidelity. Delete the directory to discard.
ABDesignSystem/Sources/Prototypes/

# Superseded v1 docs kept locally for personal reference; not part of
# the public product narrative. README does not reference them.
archive/
